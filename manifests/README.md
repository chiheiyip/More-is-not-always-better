# Local manifests

The `*.example.csv` files are synthetic input-format examples, not participant data.
Before using entry points that default to `manifests/participants.csv` and
`manifests/scene_manifest.csv`, copy the examples to those names and populate
them from your local authorized data. The populated files are ignored by Git.
Do not put real participant information in the tracked example files.
